@model IEnumerable<Kursach_Voenkomat.Models.Полы>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Пол)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Пол)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_пола">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_пола">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_пола">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
