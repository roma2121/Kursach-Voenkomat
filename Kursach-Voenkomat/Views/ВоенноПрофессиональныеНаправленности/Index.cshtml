@model IEnumerable<Kursach_Voenkomat.Models.ВоенноПрофессиональныеНаправленности>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Наименование_военно_профессиональной_направленности)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Наименование_военно_профессиональной_направленности)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_военно_профессиональной_направленности">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_военно_профессиональной_направленности">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_военно_профессиональной_направленности">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
