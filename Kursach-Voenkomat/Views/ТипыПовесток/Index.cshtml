@model IEnumerable<Kursach_Voenkomat.Models.ТипыПовесток>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Тип_повестки)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Тип_повестки)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_типа_повестки">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_типа_повестки">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_типа_повестки">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
