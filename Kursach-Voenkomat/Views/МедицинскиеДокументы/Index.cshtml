@model IEnumerable<Kursach_Voenkomat.Models.МедицинскиеДокументы>

@{
    ViewData["Title"] = "Медицинские документы";
}

<h1>Медицинские документы</h1>

<p>
    <button type="button" class="create-button" onclick="location.href='@Url.Action("Create")'">Создать</button>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Призывник)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Диагноз)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Наименование_медицинской_организации)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Дата_выписки_медицинского_заключения)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ПризывникФИО)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Диагноз)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Наименование_медицинской_организации)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Дата_выписки_медицинского_заключения)
                </td>
                <td>
                    <div class="d-flex justify-content-end">
                        <a asp-action="Edit" asp-route-id="@item.ID_медицинского_документа" class="btn btn-success">Изменить</a> |
                        <a asp-action="Details" asp-route-id="@item.ID_медицинского_документа" class="btn btn-warning">Подробности</a> |
                        <a asp-action="Delete" asp-route-id="@item.ID_медицинского_документа" class="btn btn-danger">Удалить</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
