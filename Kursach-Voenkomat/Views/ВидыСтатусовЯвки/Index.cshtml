@model IEnumerable<Kursach_Voenkomat.Models.ВидыСтатусовЯвки>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Наименование_статуса_явки)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Наименование_статуса_явки)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_наименования_статуса_явки">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_наименования_статуса_явки">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_наименования_статуса_явки">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
