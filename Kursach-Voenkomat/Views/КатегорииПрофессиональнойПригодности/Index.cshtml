@model IEnumerable<Kursach_Voenkomat.Models.КатегорииПрофессиональнойПригодности>

@{
    ViewData["Title"] = "Категории профессиональной пригодности";
}

<h1>Категории профессиональной пригодности</h1>

<p>
    <button type="button" class="create-button" onclick="location.href='@Url.Action("Create")'">Создать</button>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Категории_профессиональной_пригодности)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Категории_профессиональной_пригодности)
                </td>
                <td>
                    <div class="d-flex justify-content-end">
                        <a asp-action="Edit" asp-route-id="@item.ID_категории_профессиональной_пригодности" class="btn btn-success">Изменить</a> |
                        <a asp-action="Details" asp-route-id="@item.ID_категории_профессиональной_пригодности" class="btn btn-warning">Подробности</a> |
                        <a asp-action="Delete" asp-route-id="@item.ID_категории_профессиональной_пригодности" class="btn btn-danger">Удалить</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
