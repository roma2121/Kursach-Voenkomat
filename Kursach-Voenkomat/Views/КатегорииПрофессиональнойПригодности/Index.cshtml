@model IEnumerable<Kursach_Voenkomat.Models.КатегорииПрофессиональнойПригодности>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Категории_профессиональной_пригодности)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Категории_профессиональной_пригодности)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_категории_профессиональной_пригодности">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_категории_профессиональной_пригодности">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_категории_профессиональной_пригодности">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
